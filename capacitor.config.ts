import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TareasApp-2133000',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
